"""Concrete, replaceable implementations of the harness ports.

This package owns SDK-facing code only. Product capabilities and the harness
depend on contracts, while application wiring happens in ``src.bootstrap``.
"""
