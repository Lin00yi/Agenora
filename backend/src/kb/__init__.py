"""Knowledge Base (KB) module.

A KB is a user-owned collection of documents that get parsed, chunked,
embedded and stored as vectors in a dedicated collection (`kb_{uuid}`).
HTTP CRUD lives in ``src.api.routes.kb``.
"""
