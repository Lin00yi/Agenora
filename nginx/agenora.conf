# Deprecated static copy — production compose mounts agenora.conf.template
# and substitutes DOMAIN via nginx's envsubst entrypoint.
# Prefer editing nginx/agenora.conf.template and setting DOMAIN in .env.

# For local reference without templating, default domain:
#   server_name anykb.cc.cd
#   ssl_certificate /etc/letsencrypt/live/anykb.cc.cd/fullchain.pem

# See agenora.conf.template for the live configuration.
